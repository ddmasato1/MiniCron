# 更新日志 (Changelog)

本项目遵循 [Semantic Versioning (语义化版本 2.0.0)](https://semver.org/lang/zh-CN/) 规范。
所有版本变更记录均遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 格式。

---

## [Unreleased]

### 🐛 修复 (Fixed)
- **手动立即运行任务状态同步**：
  - 优化后端任务执行器（`TaskRunner`）结束流程，在清理运行状态和关闭 SSE 日志流前先持久化数据库执行记录，消除时序竞争。
  - 前端日志弹窗在快速失败/成功场景下主动同步任务状态，关闭弹窗时增加兜底刷新。
  - 增加运行中任务的自适应短轮询机制（2 秒间隔）与全局定时刷新，确保列表页状态实时更新。

### 🔄 变更 (Changed)
- **Docker 容器基础镜像优化**：
  - 将基础镜像由 `python:3.11-alpine` 切换为 `python:3.11-slim`，增强对复杂第三方库（如 Playwright、C/C++ 扩展包）的兼容性。
- **Web 控制台交互层级统一**：
  - 移除顶部导航中的服务器时间状态块，保留内存状态，并将任务统计集中在任务汇总区。
  - 任务、脚本、环境变量删除及 Python 依赖卸载统一使用页面内确认对话框。
  - 浏览器原生阻断式 `confirm()` / `alert()` 改为全局确认对话框与非阻断 Toast。
  - 按一级浮层 `z-50`、二级子弹窗 `z-[60]`、全局交互提示 `z-[70]` 统一设置中心与脚本管理的弹窗层级。

## [1.1.0] - 2026-09-20

### 🚀 新增 (Added)
- **系统安全关键操作独立告警**：
  - 将登录成功/失败、管理员密码修改等高危操作与日常任务通知彻底解耦。
  - 登录失败防暴力破解告警：记录并上报客户端真实 IP 溯源与时间。
  - 管理员密码变更提醒：修改密码后自动发送凭据更新确认通知。
  - 登录成功提醒：作为可选开关提供。
  - 支持独立配置 `security_chat_id`（如个人私聊），未填写时自动回退至任务主 Chat ID。
- **青龙模式纯净自定义通知**：
  - 原生兼容青龙脚本规范：支持在脚本中直接调用 `from notify import send; send(title, content)`。
  - 标准输出结构化标记协议（`__MINICRON_NOTIFY_START__...__MINICRON_NOTIFY_END__`），无损精准捕获自定义通知。
  - 纯净业务通知呈现：去除退出代码、执行耗时、触发方式等机器参数噪音，仅保留标题与内容，文末轻量标注 `🕒 时间戳 · 任务名`。
- **任务通知全新策略**：
  - 新增 `CUSTOM_ONLY`（仅脚本主动通知，推荐默认）：日常成功任务静默，脚本调 `send()` 时推送，异常崩溃自动兜底。
- **双通道独立连通性测试**：
  - Web 设置中心提供「🧪 测试任务通道」与「🛡️ 测试安全通道」两个独立测试按钮。

### 🔄 变更 (Changed)
- **任务执行结果推送重构为双轨制**：
  - 优先推送脚本自定义的业务通知；
  - 若未调用 `send()` 且任务异常失败（退出码非 0 或超时），发送精简版故障报警卡片（仅提取末尾错误摘要）。
- **Web 控制台设置中心升级**：
  - 「通知与代理」面板重新布局为「定时任务通知」与「系统安全通知」两个独立配置卡片。
- **控制台日志清洗增强**：
  - 终端查看运行日志时自动剥离通知结构化标记，保持日志洁净。

### 📝 文档 (Documentation)
- 同步更新 `README.md`、`docs/PRD.md`、`docs/ARCHITECTURE.md` 与 `docs/API_SPEC.md`。

---

## [1.0.0] - 2026-09-19

### 🚀 初始版本发布 (Initial Release)
- **超低资源占用核心架构**：单 Python 进程托管 Web API 与 APScheduler 调度引擎，空闲内存占用 < 35MB。
- **任务调度与管理**：可视化 Crontab 表达式配置、未来 5 次触发时间预演、任务一键启用/禁用、手动立即运行。
- **安全防注入沙箱**：列表参数派生子进程，彻底禁用 Shell 解释器拼接，严格校验脚本路径防路径穿越。
- **脚本在线编辑**：内置暗色代码编辑器，支持 Tab 4 格缩进、`Ctrl+S` / `Cmd+S` 快捷保存、行号统计与只读预览。
- **全局环境变量中心**：支持全局通用配置，具备多级注入与任务专属变量覆盖机制，前端敏感值掩码脱敏。
- **Python 依赖在线管理**：可视化管理 Python 包（`pip list` / `pip install` / `pip uninstall`），支持清华/阿里/腾讯国内高速镜像源与 SSE 终端流式回显。
- **智能缺失依赖诊断**：任务日志自动识别 `ModuleNotFoundError` 并提示「一键在线安装」。
- **容器重启自愈恢复**：安装包记录于 SQLite 表，Docker 重建后启动时自动补全缺失包。
- **管理员密码在线修改**：基于 PBKDF2-HMAC-SHA256（100,000 次加盐哈希）持久化存储。
- **Telegram 结果通知与出海代理**：支持 HTTP/SOCKS5 正向代理与自定义反向代理 API Base URL。
- **极简 Docker 容器化**：基于 `python:3.11-alpine` 打造 98MB 极致镜像，提供 `docker-compose.yml`。
